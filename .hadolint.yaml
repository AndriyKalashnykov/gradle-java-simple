ignored:
  - DL3006  # Always tag the version of an image explicitly — ARG-based FROM is intentional
  - DL3008  # Pin versions in apt-get install — base image handles security updates
  - DL3029  # Do not use --platform flag with FROM — explicit amd64 targeting is intentional for FIPS runtime
